# Changelog

## 0.1.0

- initialize the standalone `uggo-lint` project
- add configurable lint modes and repository config support
- improve hook and `pre-commit` integration
- improve incremental lint execution and failure output
- extend Uber Go Guide-inspired custom rules
- add CI and adoption guidance
